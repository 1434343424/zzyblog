
<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body style="background-image:url(/img/bg/slider02.jpg)">
<div id="article">
    <p>
        <% if( user && user.name == post.name ){ %>
        <span>
        <a class="edit" href="/edit/<%= post.name %>/<%= post.time.minute %>/<%= post.title %>">编辑</a>
    </span>
        <span>
        <a class="edit" href="/remove/<%= post.name %>/<%= post.time.minute %>/<%= post.title %>">删除</a>
    </span>
        <span>
        <a class="edit" href="/wenzhang">返回Blog</a>
    </span>
        <% } %>
    </p>
    <p class="info">
    <p>作者：<a href="/archive"><%= post.name %></a></p>
    <p>日期：<%= post.time.minute %></p>
    <p>标签：
        <% post.tags.forEach(function (tag, index) { %>
        <% if (tag) { %>
        <a class="tag" href="/tags"><%= tag %></a>
        <% } %>
        <% }) %>
    </p>
    <p><%- post.post %></p>
    <p class="info">阅读：<%= post.pv %> | 评论：<%= post.comments.length %></p>
    <% include comment %>
</div>
</body>
</html>